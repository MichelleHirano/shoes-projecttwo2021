
    <div>
        <h2 class="page-title">Your Posts</h2>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="pure-button">Edit</button></a>
        </div>
    {{/each}}

{{else}}
<h6 >you do not currently have any posts</h6>

{{/if}}